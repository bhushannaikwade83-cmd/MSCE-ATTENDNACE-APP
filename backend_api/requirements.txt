# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Face Recognition - RetinaFace (detection) + ArcFace (embedding)
# InsightFace provides both RetinaFace detector and ArcFace embedding model
insightface==0.7.3  # Includes RetinaFace (detection) + ArcFace R100 (512-dim embeddings)
onnxruntime==1.16.3  # ONNX Runtime for InsightFace models (CPU version for Cloud Run)
opencv-python-headless==4.8.1.78  # Headless version for Cloud Run (no GUI)
numpy==1.24.3
Pillow==10.1.0
scipy>=1.9.0  # Required by some ML libraries

# Vector Database
faiss-cpu==1.7.4  # Use faiss-gpu if you have GPU
# faiss-gpu==1.7.4  # Uncomment if using GPU

# Firebase (for metadata)
firebase-admin==6.2.0
google-auth>=2.0.0  # Required by firebase-admin

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
requests>=2.28.0  # For downloading models