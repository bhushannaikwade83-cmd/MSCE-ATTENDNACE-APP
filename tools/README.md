# Tools (Flutter repo)

Web admin portals were moved to a **separate folder** next to this app:

**`../msce-website/`**

- `admin-portal-react` — React admin portal  
- `admin-approval-portal` — static approval portal  

This `tools/` directory now only holds scripts used with the Flutter project (e.g. `generate_msce_workflow_docx.py`).
